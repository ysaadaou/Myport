import React from 'react'

function Head({arg}) {
  return (
    <div>
      <span style={{color: "cyan"}}>ysaadaou</span> Le <span style={{color:"yellow"}}>(2022)</span> in <span style={{color: 'lime'}}>{arg}</span>
    </div>
  )
}

export default Head
